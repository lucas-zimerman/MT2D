/*====================================================================================
generic_integer_reader.h / generic_integer_reader.cpp created By Lucas Zimerman Fraulob

	This file will read a string (numbers chars and minus signal only) and will return 
	the integer value of it.
	That function doesn't check the size of the string, so it may end into an overflow
	(To be fixed)
=======================================================================================*/


int MT2D_InputBox_Integer(char *mensage);//A generic box that read the user string and send for you...
